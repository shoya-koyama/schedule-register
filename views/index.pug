extends layout

block content
  div.container.my-3
    div.p-5.bg-light.rounded-3
      h1.text-body 予定調整アプリ
      p.lead これは、GitHub で認証でき、予定を作って出欠が取れるサービスである。
  div.container
    if user
      a.btn.btn-primary(href="/schedules/new") 予定を作る
      - const hasSchedule = schedules.length > 0
      if hasSchedule
        h3.my-3 貴殿の作った予定一覧
        table.table
          tr
            th 予定名
            th 更新日時
          each schedule in schedules
            tr
              td
                a(href=`/schedules/${schedule.scheduleId}`) #{schedule.scheduleName}
              td #{schedule.formattedUpdatedAt}
